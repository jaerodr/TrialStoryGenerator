#pragma once
#ifndef ENUMS_H
#define ENUMS_H

/*  Create an enum to simplify the processing of character data. */
enum little_red_enum {RED_AGE = 1, RED_PLACE_OF_RESIDENCE, RED_PARENTS, RED_CHARACTER_DESCRIPTIONS, RED_LIKES, RED_DECISIONS};

#endif